#include <iostream>
#include <algorithm>
using namespace std;
int n;
long long dp[91];
int main() {
	cin >> n;
	dp[1] = dp[2] = 1;
	for (int i = 3; i <= n; i++) {
		dp[i] = dp[i - 2] * 2 + dp[i - 3];
	}
	cout << dp[n] << endl;
}