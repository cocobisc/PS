#include <cstdio>
int main() {
	int n; scanf("%d", &n);
	long long ans = 1;
	int cnt = 0;
	for (int i = 0; i < n; i++) {
		int a; scanf("%d", &a);
		if ((a && !cnt) || (!a && cnt)) cnt++;
		else if (a && cnt) {
			ans *= cnt;
			cnt = 1;
		}
	}
	printf("%lld", ans);
}